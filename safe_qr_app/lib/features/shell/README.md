# Feature: Shell (3 abas)

- **Responsabilidade:** `IndexedStack` com **Ler / Gerar / Histórico**, `NavigationBar` e ações globais (ex.: ciclo de tema).
- **Apresentação:** `MainShellPage` (sem regra de negócio).
- **Testes (smoke):** `test/features/shell/shell_smoke_test.dart`.
