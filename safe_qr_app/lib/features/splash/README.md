# Feature: Splash

- **Responsabilidade:** primeiro ecrã com identidade e transição suave para o shell; sem lógica de autenticação.
- **Apresentação:** `SplashPage` (timer + `pushReplacement` para o shell).
- **Testes (smoke):** `test/features/splash/splash_smoke_test.dart`.
