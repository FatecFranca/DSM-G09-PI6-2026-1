# Copie para .env (não commitar): cp .env.example .env

# Dev local — URL base da API (sem barra final, sem /v1/...)
VITE_API_BASE_URL=http://localhost:3000

# Produção (build): copie para .env.production antes do deploy
# VITE_API_BASE_URL=https://safe-qr-api-iw32tfemba-rj.a.run.app

# Chave admin — opcional em dev (senão digite no login)
# NUNCA commite a chave real — use ADMIN_API_KEY no safe_qr_back
# VITE_ADMIN_API_KEY=sua-chave-admin-aqui
